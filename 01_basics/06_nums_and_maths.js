//***************NUMS******************* */\
const score = 400
console.log(score);


const balance = new Number(100) // coonnfirms that the number is in integer
console.log(balance);
 
console.log(balance.toString().length); // now the number is converted to string 
console.log(balance.toFixed(2)); // adds decimal points 


const hundreds = 1000000
console.log(hundreds.toLocaleString()); //adds commas

// **************MAthS***************

console.log(Math.abs(-4)); // prints in positive

