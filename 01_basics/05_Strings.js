const name = "numan"
const repoCount = 50
// console.log(name   +   repoCount); //old versionn of adding strings

console.log(`Hello my name is ${name} and my repository count is ${repoCount} and upper case of name is ${name.toUpperCase()}`);

console.log(name.charAt(2));
console.log(name.indexOf('n'));
console.log(name.substring(0,3));
 

const newString = "numan-raza-ziyai"
console.log(newString.split('-')); // split the string in array form 


